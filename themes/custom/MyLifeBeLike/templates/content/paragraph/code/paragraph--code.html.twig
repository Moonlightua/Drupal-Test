{% set bem_block = 'paragraph-code' %}

<div class="{{ bem_block }}">
 <div class="{{ bem_block }}__title">
   {{ content.field_title }}
 </div>
  <div class="{{ bem_block }}__content">
    {{ content.field_content }}
  </div>

</div>
